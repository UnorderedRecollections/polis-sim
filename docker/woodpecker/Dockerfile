# Woodpecker server — the CI coordinator; the Mechanical Magistrate's
# engine room. OAuth against gitea; agents connect over gRPC :9000.
# API base is /api (unversioned), Bearer auth.
FROM woodpeckerci/woodpecker-server:v3

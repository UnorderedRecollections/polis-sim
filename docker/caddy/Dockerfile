# caddy — the front proxy (task 0042): the single entrypoint to a
# deployment's services, and the only container publishing a host port.
# The Caddyfile is generated per sim at provisioning (route prefixes and
# upstream names depend on the sim) and mounted at /etc/caddy/Caddyfile.
FROM docker.io/library/caddy:2-alpine

# Gogs — the phase-1 platform ("customary machinery").
# In the fiction: merely the mechanical host of the legal archive. It knows
# git refs and nothing of petitions, proceedings or approvals.
# "next" build: gitea-shaped v1 API, but NO pulls API at all and repo
# creation with auto_init fails — see docs/services/gogs.md.
FROM gogs/gogs:next-latest

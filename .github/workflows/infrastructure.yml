name: infrastructure tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  behave:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync --frozen
      # the runner's docker backs the shared-sim failure scenarios
      - name: infrastructure domain
        run: uv run behave features/ --tags @infrastructure
      - name: container diagnostics (on failure)
        if: failure()
        run: |
          docker ps -a
          for c in $(docker ps -a --format '{{.Names}}' | grep -E '^(bdd-|fail-)' || true); do
            echo "===== logs: $c"
            docker logs --tail 80 "$c" 2>&1 || true
          done
